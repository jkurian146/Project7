package controller;

import model.GameState;

public interface IModelStatus {
  
  // that returns gamestate
  // returns pop up for if the game is over
  // specific to if player 1 won, player 2 won or if it's a tie
}
