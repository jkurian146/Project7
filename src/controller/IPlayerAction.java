package controller;

public interface IPlayerAction {
  // illegal move
  // display pop up notifying user that an llegal move was attempted

  // called when the player has been switched
  // switch player
}
